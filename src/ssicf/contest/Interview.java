package ssicf.contest;


import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class Interview {
  public static void main(String[] args) {
//    InterviewSolution s = new InterviewSolution();
//    int[][] nums = new int[][]{{1, 3}, {2, 6}, {8, 10}, {15, 18}};
//    List<Interval> result = s.merge(nums);
//    System.out.println(result);
  }
}

class InterviewSolution {
//  List<Interval> merge(List<Interval> intervals) {
//    Arrays.sort(intervals, (a, b) -> {return a.start > b.start;});
////    Interval head = intervals.get(0);
//    List<Interval> res = new ArrayList<>();
//    res.add(new Interval(intervals.get(0).start, intervals.get(0).end));
//    for(int i=1; i<intervals.size(); i++){
//      Interval last = res.get(res.size()-1);
//      if(intervals.get(i).start <= last.end)
//        last.end = Math.max(intervals.get(i).end, last.end);
//      else
//        res.add(intervals.get(i));
//    }
//    return res;
//  }
}

//class Interval {
//  int start;
//  int end;
//
//  public Interval(int start, int end) {
//    this.start =
//  }
//}

